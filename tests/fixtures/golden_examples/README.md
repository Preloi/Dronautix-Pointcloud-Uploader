These example fixtures are scaffolding for the V2 Golden Master plan.

They are intentionally small hand-written samples that exercise volatile fields,
generated IDs, Unicode text, floating point normalization, CRS metadata, and the
`cloud.js = {...};` wrapper. They do not replace the final Golden Masters, which
must be captured from real outputs of the legacy app before behavior is moved.

